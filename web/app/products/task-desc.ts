/* =========================================================================
   9（＋非公開2）タスクの 1 行説明 — 表示コピーの単一ソース（SSOT）

   なぜ独立ファイルなのか:
     この文言は catalog.tsx（商品面）と makers.ts（メーカー面）の両方が使う。
     以前は両ファイルに同じ文をコピーしていて、solid / rotate / decompose /
     fold の 4 本が乖離し、うち decompose と rotate は pack-tasks の定義と
     食い違ったまま商品面に出ていた（2026-08-08 に是正）。
     makers.ts は API ルート（Stripe webhook ほか 4 本）からも import される
     ため、makers.ts → catalog.tsx と繋ぐと React コンポーネント群がサーバー
     バンドルへ入る。そこで文言だけを依存ゼロの葉モジュールに切り出した。

   文体について（重要・2026-08-08）:
     9 本すべてを「13〜18 字・常体・〜て、〜る。」で揃えていたため、機械が
     生成した一覧に見えていた。意図的にムラを作るのが処方なので、
     **rotate / decompose / fold の 3 本だけ**を長さの違う敬体にしてある。
     残り 6 本は基準文として常体のまま据え置く。
     ここを「全部そろえたい」と感じたら、それは症状の再発。触らないこと。

   出典: pack-tasks.md §15-§22（各タスクの定義）／pack-design.md §11.6
   ========================================================================= */

import type { MakerKey } from "./capabilities";

export const TASK_DESC: Record<MakerKey, string> = {
  /* ---- 基準文（常体・短文）。ここは揃っていてよい ---- */
  copy: "見本のとおりに、点をつないで写す。",
  solid: "平面の点から、立体を起こして写す。",
  fill: "足りない辺を補って、形を閉じる。",
  mirror: "鏡の反対側に映る形を描く。",
  translate: "形を変えずに、ずらして写す。",
  overlay: "2 つの形を重ねたところを描く。",

  /* ---- 型を崩す 3 本（敬体・長さ不揃い） ---- */
  // 旧「回しても同じ形だととらえる。」は回転不変性の説明で、子がやる作業
  // （回した形を描く）と食い違っていた。pack-tasks §16 に合わせて是正。
  rotate: "紙を回さずに、頭の中で回して書きます。9 種類のなかで、いちばん手ごわい。",
  // 旧「1 つの形を、パーツに分ける。」は分割の説明。実際は C − B ＝ A の
  // 引き算（かさねの逆操作）。pack-tasks §20 に合わせて是正。
  decompose: "かさねの逆。重なった形から、片方だけを取り除いて描きます。",
  fold: "折り紙のように折って、重なったところを描きます。",

  /* ---- ローンチ非公開（LAUNCH_HIDDEN）。データは温存 ---- */
  scale: "比をそろえて、大きく写す。",
  shrink: "比をそろえて、小さく写す。",
};
